//interface de los estudiantes con los datos que solo
//queremos usar de todo lo que nos viene de la api
export interface mySoon {
    name: string;
    avatar: string;
    surname: string;
    id: string;
}